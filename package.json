{
  "name": "okland.org-static-page",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir docs 2> /dev/null; cp src/*.html src/*.js src/*.svg src/*.png src/*.css docs; sass src/particle.scss docs/particle.css",
    "build:watch": "watch 'npm run build' src",
    "clean": "rm -rf docs",
    "serve": "http-server docs"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^6.6.0",
    "eslint-config-airbnb-base": "^14.0.0",
    "eslint-plugin-import": "^2.18.2",
    "sass": "^1.22.12",
    "watch": "^1.0.2"
  }
}
