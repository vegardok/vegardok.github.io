{
  "name": "okland.org-static-page",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir out 2> /dev/null; cp src/*.html src/*.svg src/*.png src/*.css out; sass src/particle.scss out/particle.css",
    "build:watch": "watch 'npm run build' src",
    "serve": "http-server out"

  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.22.12"
  }
}
